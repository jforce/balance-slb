apiVersion: machineconfiguration.openshift.io/v1
kind: MachineConfig
metadata:
  labels:
    machineconfiguration.openshift.io/role: worker
  name: 99-ovs-balance-slb
spec:
  config:
    ignition:
      version: 3.2.0
    storage:
      files:
        # I'd need custom machineconfig pools right?
        - path: /etc/nmstate/openshift/<hostname>.yml
          mode: 0644
          overwrite: true
          contents:
            source: data:text/plain;charset=utf-8;base64,xxxxxx
